PORT=3000
## IMPORTANT: replace the password placeholder below with the real password
## Do NOT include angle brackets. If your password contains special characters
## (e.g. @, :, /, ? , #, or space) URL-encode them (use %40 for @, etc.).
MONGODB_URI=mongodb+srv://abhishekadmin:abh123@devlovers.hkjdugc.mongodb.net/DevLovers?retryWrites=true&w=majority&appName=DevLovers
## Optional: a friendly name to display in logs instead of the Atlas host
## Example: MONGODB_HOST_ALIAS=DevLovers-Atlas
MONGODB_HOST_ALIAS=ac-unznoxh-shard-00-00.hkjdugc.mongodb.net
JWT_SECRET=your-super-secret-jwt-key-here
JWT_EXPIRE=7d
NODE_ENV=development

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=abhishekbuisness7985@gmail.com
## If using Gmail: create an App Password (recommended) and paste it here WITHOUT spaces.
## Many people copy App Passwords with spaces (e.g. "abcd efgh ijkl mnop") — those
## spaces will be removed automatically by the server, but it's best to paste the
## contiguous app-password value (e.g. abcd efgh ijkl mnop -> abcdefghijklmnop URL-encode if needed).
## Alternatively, configure OAuth2 vars below and leave SMTP_PASS blank.
SMTP_PASS=fzngvkhnwkrfufhz
SMTP_FROM="DevLovers <abhishekbuisness7985@gmail.com>"

## Optional OAuth2 (Gmail) configuration - use instead of SMTP_PASS for higher security.
## SMTP_OAUTH_CLIENT_ID=your-google-client-id
## SMTP_OAUTH_CLIENT_SECRET=your-google-client-secret
## SMTP_OAUTH_REFRESH_TOKEN=your-refresh-token
## SMTP_OAUTH_USER=abhishekbuisness795@gmail.com


